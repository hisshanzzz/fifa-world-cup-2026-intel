# Copy to .env and fill in IF you have paid X/Twitter API access.
# Everything runs without these using bundled sample data.
X_BEARER_TOKEN=
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_SECRET=

# Sentiment backend: "hf" (HuggingFace) or "vader" (no torch needed)
SENTIMENT_BACKEND=vader
