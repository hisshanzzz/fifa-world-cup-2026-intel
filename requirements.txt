# Core data stack
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3

# Dashboards / viz
streamlit>=1.30
plotly>=5.18
dash>=2.14

# Sentiment (HuggingFace primary, VADER fallback so it runs without torch)
transformers>=4.38 ; python_version < "3.14"
torch>=2.2 ; python_version < "3.14"
vaderSentiment>=3.3.2

# Optional live tweet ingestion (only used if X API keys are present)
tweepy>=4.14

python-dotenv>=1.0
